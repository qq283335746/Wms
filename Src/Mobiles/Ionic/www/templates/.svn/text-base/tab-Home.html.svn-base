<ion-view view-title="智能仓储配送平台">
    <ion-content class="home">
        <ion-list>
            <ion-item class="item item-icon-left item-icon-right" ng-repeat="item in HomeMenuItems" href="{{item.Url}}">
                <i class="icon"><img src="{{item.Src}}" /></i>{{item.Name}}
                <i class="icon ion-ios-arrow-right"></i>
            </ion-item>
        </ion-list>
    </ion-content>
    <!--<ion-content class="home">
        <div class="icons-rows">
            <div class="row" ng-repeat="item in ListNav">
                <div class="col{{item.IsBorder1}}">
                    <a href="{{item.Url1}}">
                        <img src="{{item.Src1}}" />
                        <p>{{item.Name1}}</p>
                    </a>
                </div>
                <div class="col{{item.IsBorder2}}">
                    <a href="{{item.Url2}}">
                        <img src="{{item.Src2}}" />
                        <p>{{item.Name2}}</p>
                    </a>
                </div>
                <div class="col{{item.IsBorder3}}">
                    <a href="{{item.Url3}}">
                        <img src="{{item.Src3}}" />
                        <p>{{item.Name3}}</p>
                    </a>
                </div>
            </div>
        </div>

    </ion-content>-->
</ion-view>
